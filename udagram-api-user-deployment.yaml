apiVersion: apps/v1
kind: Deployment
metadata:
  name: udagram-api-user-deployment
  labels:
    app: udagram-api-user
spec:
  replicas: 2
  selector:
    matchLabels:
      app: udagram-api-user
  template:
    metadata:
      labels:
        app: udagram-api-user
    spec:
      containers:
      - name: udagram-api-user-deployment
        image: nghiahv1/udagram-api-user:v1
        ports:
        - containerPort: 8080