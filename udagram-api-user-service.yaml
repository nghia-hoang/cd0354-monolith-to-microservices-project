apiVersion: v1
kind: Service
metadata:
  name: udagram-api-user-deployment
  labels:
    run: udagram-api-user-deployment
spec:
  ports:
  - port: 8080
    protocol: TCP
  selector:
    run: udagram-api-user-deployment