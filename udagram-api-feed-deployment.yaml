apiVersion: apps/v1
kind: Deployment
metadata:
  name: udagram-api-feed-deployment
  labels:
    app: nodeapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nodeapp
  template:
    metadata:
      labels:
        app: nodeapp
    spec:
      containers:
      - name: udagram-api-feed-deployment
        image: udagram-api-feed:v1
        ports:
        - containerPort: 8080