apiVersion: apps/v1
kind: Deployment
metadata:
  name: udagram-api-user-deployment
  labels:
    app: node-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: node-app
  template:
    metadata:
      labels:
        app: node-app
    spec:
      containers:
      - name: udagram-api-user-deployment
        image: nghiahv1/udagram-api-user:v1
        ports:
        - containerPort: 8080